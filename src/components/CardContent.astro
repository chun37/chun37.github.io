---
interface Props {
  title: string;
  body: string;
}

const { title, body } = Astro.props;
---

<div>
  <div class="flex flex-col p-2">
    <h3 class="text-xl leading-none">{title}</h3>
  </div>
  <div class="p-2 pt-0 text-sm text-slate-500">{body}</div>
</div>
